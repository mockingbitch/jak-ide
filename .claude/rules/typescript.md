Never use any.

Prefer unknown.

Use Result pattern.

Strict mode enabled.

Avoid class unless necessary.

No default export.

Prefer named export.

Prefer readonly.

Prefer immutable objects.

Every async function must handle errors.

Never use nested Promise.