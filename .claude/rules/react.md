Use Functional Components.

Never use Class Component.

Use TanStack Query.

Use Zustand.

Use React Hook Form.

One component = one responsibility.

No business logic inside UI.

Maximum 250 LOC per component.